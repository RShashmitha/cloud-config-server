 
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
      prefer-ip-address: true

rest:
  base-url: http://PAYMENT-SERVICE/payment/

actor:
  service:
    url: http://localhost:8081/actors
